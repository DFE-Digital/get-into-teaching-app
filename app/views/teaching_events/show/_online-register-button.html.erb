<div class="register">
  <%= link_to event_steps_path(@event.readable_id, channel: params[:channel], sub_channel: params[:sub_channel]), class: %w[button] do %>
    Register for this event
  <% end %>
</div>
