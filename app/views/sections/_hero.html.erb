<% if image === false %>
<div class="no-hero"></div>
<% else %>
<div class="hero" style="background-image: url('<%= hero_image_path image %>')">
    <div class="hero__banner">
        <div class="hero__banner__text">
            <h1><%= header %></h1>
        </div>
    </div>
</div>
<% end %>
