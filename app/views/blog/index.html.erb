<ol class="blog-index">
  <% @posts.each do |path, fm| %>
    <li>
      <h2><%= fm[:title] %></h2>
      <% if fm[:images].present? %>
        <%= image_pack_tag(fm[:images]&.values&.first) %>
      <% end %>
      <p><%= fm[:synopsis] %></p>

      <%= link_to "Read more", path %>
    </li>
  <% end %>
</ol>
