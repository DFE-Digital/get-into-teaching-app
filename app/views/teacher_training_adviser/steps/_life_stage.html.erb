<%= f.govuk_collection_radio_buttons :situation,
                                     f.object.situations,
                                     :id,
                                     ->(option) { format_option_value(option, "teacher_training_adviser_steps.life_stage.situation") },
                                     ->(option) { format_option_hint(option, "teacher_training_adviser_steps.life_stage.situation") },
                                     legend: { tag: "h1", text: t('helpers.label.teacher_training_adviser_steps_life_stage.situation', **Value.data) },
                                     hint: { text: t('helpers.hint.teacher_training_adviser_steps_life_stage.situation', **Value.data) }
%>
