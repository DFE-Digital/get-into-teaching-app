<%= f.govuk_collection_radio_buttons :degree_country_id,
                                     f.object.options,
                                     :last,
                                     ->(option) { t("helpers.answer.teacher_training_adviser_steps.degree_country.degree_country_id.#{option.last}") },
                                     legend: { tag: "h1" } %>
